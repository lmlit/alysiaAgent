{
  "name": "alysia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "pnpm -r build",
    "dev:server": "pnpm --filter @alysia/server dev"
  },
  "devDependencies": {
    "@rolldown/binding-win32-x64-msvc": "^1.1.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
